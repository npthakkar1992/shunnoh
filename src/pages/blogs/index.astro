---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Nav from "../../components/Nav.astro";
import Footer from "../../components/Footer.astro";
import BlogCard from "../../components/BlogCard.astro";
import ScrollReveal from "../../components/ScrollReveal.astro";
import { blogCards } from "../../data/blogCards";
---
<BaseLayout title="Stories — Shunnoh" description="Immersive travel stories from Varanasi, Goa, and beyond.">
  <Nav current="blogs" />

  <!-- Header -->
  <section class="bg-parchment">
    <div class="mx-auto max-w-3xl px-6 sm:px-8 lg:px-10 text-center py-28 sm:py-36">
      <h1 class="hero-load hero-load-1 font-display text-ink" style="font-size:clamp(2.8rem,5.5vw,4.2rem);letter-spacing:-0.02em;line-height:1.1;">Blogs</h1>
      <p class="hero-load hero-load-2 font-body text-stone mt-6 mx-auto" style="font-size:clamp(1.05rem,1.4vw,1.2rem);line-height:1.85;font-weight:300;max-width:640px;">
        Explore travel through a different lens—where the journey matters as much as the destination. Shunnoh is a space for slow, meaningful exploration, uncovering the soul of every place beyond the tourist map. From hidden gems to transformative experiences, each story here is an invitation to connect, feel, and truly belong wherever you wander.
      </p>
    </div>
  </section>

  <!-- Blog cards -->
  <section class="mx-auto max-w-6xl px-6 sm:px-8 lg:px-10 py-28 sm:py-32">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-10 md:gap-12">
      {blogCards.map((card) => (
        <ScrollReveal>
          <BlogCard
            title={card.title}
            subtitle={card.subtitle}
            href={card.href}
            imageSrc={card.imageSrc}
            imageAlt={card.imageAlt}
          />
        </ScrollReveal>
      ))}
    </div>
  </section>

  <Footer />
</BaseLayout>
